# 本機 Java jar

這個資料夾放本機執行需要的 Java library。

需要的 jar：

```text
lib/mysql-connector-j-*.jar
lib/flatlaf-*.jar
```

用途：

- `mysql-connector-j-*.jar`：Java 連接 MySQL 使用。
- `flatlaf-*.jar`：讓 Swing 介面使用現代化 Look and Feel。

這個資料夾裡的 jar 檔已經被 Git 忽略，所以換電腦時要重新放一次。
